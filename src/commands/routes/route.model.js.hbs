const { DataTypes } = require( "sequelize" );
const { sequelize } = require( "./db" );

// MAWORK CLI AJOUT IMPORT NE PAS TOUCHER

const {{pascalCase routeName}} = sequelize.define(
	"{{pascalCase routeName}}",
	{
		id      : {
			primaryKey   : true,
			type         : DataTypes.UUID,
			defaultValue : DataTypes.UUIDV4
		}
	}
);

// MAWORK CLI AJOUT ASSOCIATIONS NE PAS TOUCHER

module.exports = {{pascalCase routeName}};
