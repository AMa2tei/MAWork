const request = require( "supertest" );
const path    = require( "path" );
const dotenv  = require( "dotenv" ).config( {
	                                            path : path.resolve(
		                                            ".env.tests"
	                                            )
                                            } );
describe(
	"Tests pour les routes '{{pascalCase routeName}}'",
	function () {
		test(
			"Premier test",
			async () => {
				// TODO : Écrire le test
			}
		);
	}
);